
<div class="jumbotron">
  <h1>Uneaten Burger</h1>
  {{#each udBurger}}
    <div>
      <h2>Burger Name: {{burger_name}} </h2>
    </div>
  {{/each}}
  <button id ='devourButton' value = {{udBurger.0.id}}>Devour</button>
  <h1>Eaten Burger</h1>
  {{#each dBurger}}
  <div>
    <h2>Burger Name: {{burger_name}} </h2>
  </div>
  {{/each}}
  <button id ='regurgitate' value = {{dBurger.0.id}}>Regurgitate</button>

  <form id="newBurger">
    New Burger: <input type="text" id="newBurgerInp"></br>
    <input type="submit" name="submitButton" id="postSubmit">
  </form>

</div>


<script src="./assets/javascript/index.js"></script>
